{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$id": "http://example.com/bookSearch.json",
	"type": "object",
	"properties": {
		"query": {
			"type": "string",
			"description": "The search query to be used to search for books and authors."
		},
		"limit": {
			"type": "integer",
			"description": "The maximum number of results to return."
		},
		"fields": {
			"type": "array",
			"description": "The fields to return in the search results.",
			"items": {
				"type": "string"
			}
		}
	},
	"required": [ "query" ],
	"additionalProperties": false
}
